const fs = require('fs');
const filepath = 'c:/Users/hp/Desktop/Olitech CoffeeShop/CoffeeShop_Frontend/src/pages/shop/Owner.jsx';
const content = fs.readFileSync(filepath, 'utf8');

// Find the marker: after opening the am-eod-report div, before first section
const marker = '<div className="am-eod-report">\r\n                {/* \u2500\u2500 Current Stock Levels \u2500\u2500 */}';
const markerIdx = content.indexOf(marker);

if (markerIdx === -1) {
  console.log('Marker not found! Trying alternate...');
  // Try without CRLF
  const alt = '<div className="am-eod-report">';
  const altIdx = content.indexOf(alt);
  console.log('Alt marker at:', altIdx);
  console.log('Context:', JSON.stringify(content.slice(altIdx, altIdx + 200)));
  process.exit(1);
}

console.log('Marker found at index:', markerIdx);

const newSection = `<div className="am-eod-report">\r\n\r\n                {/* \u2500\u2500 Completed Orders (by date) \u2500\u2500 */}\r\n                {(() => {\r\n                  const seenOrders = new Set();\r\n                  const completedOrders = dailyRows\r\n                    .filter(row => {\r\n                      if (!row.orderId || seenOrders.has(row.orderId)) return false;\r\n                      seenOrders.add(row.orderId);\r\n                      return true;\r\n                    })\r\n                    .sort((a, b) => new Date(a.at) - new Date(b.at));\r\n\r\n                  return (\r\n                    <section className="am-eod-section">\r\n                      <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 16 }}>\r\n                        <h3 className="am-eod-section-title" style={{ margin: 0 }}>\r\n                          \u2705 Completed Orders\r\n                        </h3>\r\n                        <div style={{ display: 'flex', gap: 16, alignItems: 'center' }}>\r\n                          <span style={{ fontSize: 13, color: '#64748B' }}>\r\n                            <strong style={{ color: '#1D3557', fontSize: 15 }}>{completedOrders.length}</strong> orders\r\n                          </span>\r\n                          <span style={{ fontSize: 13, color: '#64748B' }}>\r\n                            Total: <strong style={{ color: '#16A34A', fontSize: 15 }}>{Number(eodRevenue).toLocaleString()} RWF</strong>\r\n                          </span>\r\n                        </div>\r\n                      </div>\r\n\r\n                      {completedOrders.length === 0 ? (\r\n                        <div className="am-eod-empty" style={{ padding: '40px 0', textAlign: 'center' }}>\r\n                          <div style={{ fontSize: 32, marginBottom: 8 }}>\ud83d\udccb</div>\r\n                          <div style={{ color: '#94A3B8', fontSize: 14 }}>No completed orders for {new Date(reportDay).toLocaleDateString('en-GB', { timeZone: 'Africa/Kigali' })}</div>\r\n                        </div>\r\n                      ) : (\r\n                        <div style={{ overflowX: 'auto' }}>\r\n                          <table style={{ width: '100%', borderCollapse: 'collapse', fontSize: 13 }}>\r\n                            <thead>\r\n                              <tr style={{ background: '#F8FAFC', borderBottom: '2px solid #E2E8F0' }}>\r\n                                <th style={{ padding: '10px 14px', textAlign: 'left', color: '#64748B', fontWeight: 700, fontSize: 11, textTransform: 'uppercase', letterSpacing: '0.05em', whiteSpace: 'nowrap' }}>#</th>\r\n                                <th style={{ padding: '10px 14px', textAlign: 'left', color: '#64748B', fontWeight: 700, fontSize: 11, textTransform: 'uppercase', letterSpacing: '0.05em', whiteSpace: 'nowrap' }}>Time</th>\r\n                                <th style={{ padding: '10px 14px', textAlign: 'left', color: '#64748B', fontWeight: 700, fontSize: 11, textTransform: 'uppercase', letterSpacing: '0.05em' }}>Items Ordered</th>\r\n                                <th style={{ padding: '10px 14px', textAlign: 'left', color: '#64748B', fontWeight: 700, fontSize: 11, textTransform: 'uppercase', letterSpacing: '0.05em', whiteSpace: 'nowrap' }}>Served By</th>\r\n                                <th style={{ padding: '10px 14px', textAlign: 'left', color: '#64748B', fontWeight: 700, fontSize: 11, textTransform: 'uppercase', letterSpacing: '0.05em', whiteSpace: 'nowrap' }}>Payment</th>\r\n                                <th style={{ padding: '10px 14px', textAlign: 'right', color: '#64748B', fontWeight: 700, fontSize: 11, textTransform: 'uppercase', letterSpacing: '0.05em', whiteSpace: 'nowrap' }}>Amount</th>\r\n                              </tr>\r\n                            </thead>\r\n                            <tbody>\r\n                              {completedOrders.map((row, idx) => {\r\n                                const time = row.at\r\n                                  ? new Date(row.at).toLocaleTimeString('en-GB', { hour: '2-digit', minute: '2-digit', timeZone: 'Africa/Kigali' })\r\n                                  : '\u2014';\r\n                                const methodColors = {\r\n                                  'Cash':    { bg: 'rgba(16,185,129,0.1)', color: '#059669' },\r\n                                  'MoMo':    { bg: 'rgba(56,189,248,0.1)', color: '#0284C7' },\r\n                                  'POS':     { bg: 'rgba(139,92,246,0.1)', color: '#7C3AED' },\r\n                                  'CR/Loan': { bg: 'rgba(245,158,11,0.1)', color: '#D97706' },\r\n                                };\r\n                                const mc = methodColors[row.methodLabel] || { bg: '#F1F5F9', color: '#475569' };\r\n                                return (\r\n                                  <tr\r\n                                    key={row.orderId || idx}\r\n                                    style={{ borderBottom: '1px solid #F1F5F9', background: idx % 2 === 0 ? '#FFFFFF' : '#FAFBFC', transition: 'background 0.15s' }}\r\n                                    onMouseEnter={e => e.currentTarget.style.background = '#EFF6FF'}\r\n                                    onMouseLeave={e => e.currentTarget.style.background = idx % 2 === 0 ? '#FFFFFF' : '#FAFBFC'}\r\n                                  >\r\n                                    <td style={{ padding: '10px 14px', color: '#94A3B8', fontWeight: 600, fontSize: 12 }}>{idx + 1}</td>\r\n                                    <td style={{ padding: '10px 14px', whiteSpace: 'nowrap' }}>\r\n                                      <div style={{ fontWeight: 700, color: '#1D3557', fontSize: 14 }}>{time}</div>\r\n                                      <div style={{ fontSize: 10, color: '#94A3B8', marginTop: 2 }}>\r\n                                        {row.at ? new Date(row.at).toLocaleDateString('en-GB', { day: '2-digit', month: 'short', timeZone: 'Africa/Kigali' }) : ''}\r\n                                      </div>\r\n                                    </td>\r\n                                    <td style={{ padding: '10px 14px', maxWidth: 340 }}>\r\n                                      {(row.rawItems || []).length > 0 ? (\r\n                                        <div style={{ display: 'flex', flexWrap: 'wrap', gap: '4px 8px' }}>\r\n                                          {(row.rawItems || []).map((it, i) => (\r\n                                            <span key={i} style={{ background: '#F1F5F9', color: '#334155', padding: '2px 8px', borderRadius: 20, fontSize: 11, fontWeight: 600, border: '1px solid #E2E8F0', whiteSpace: 'nowrap' }}>\r\n                                              {it.qty || 1}\u00d7 {it.name}\r\n                                            </span>\r\n                                          ))}\r\n                                        </div>\r\n                                      ) : (\r\n                                        <span style={{ color: '#94A3B8', fontStyle: 'italic' }}>{row.items || 'No details'}</span>\r\n                                      )}\r\n                                    </td>\r\n                                    <td style={{ padding: '10px 14px', whiteSpace: 'nowrap' }}>\r\n                                      <div style={{ display: 'flex', alignItems: 'center', gap: 6 }}>\r\n                                        <div style={{ width: 28, height: 28, borderRadius: '50%', background: '#1D3557', color: '#FFF', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: 10, fontWeight: 800, flexShrink: 0 }}>\r\n                                          {(row.waiterName || 'S').split(' ').map(n => n[0]).join('').toUpperCase().slice(0, 2)}\r\n                                        </div>\r\n                                        <span style={{ fontWeight: 600, color: '#334155', fontSize: 12 }}>{row.waiterName || '\u2014'}</span>\r\n                                      </div>\r\n                                    </td>\r\n                                    <td style={{ padding: '10px 14px', whiteSpace: 'nowrap' }}>\r\n                                      <span style={{ background: mc.bg, color: mc.color, padding: '3px 10px', borderRadius: 20, fontSize: 11, fontWeight: 700 }}>\r\n                                        {row.methodLabel || '\u2014'}\r\n                                      </span>\r\n                                    </td>\r\n                                    <td style={{ padding: '10px 14px', textAlign: 'right', whiteSpace: 'nowrap' }}>\r\n                                      <span style={{ fontWeight: 800, color: '#1D3557', fontSize: 14 }}>{Number(row.amount || 0).toLocaleString()}</span>\r\n                                      <span style={{ color: '#94A3B8', fontSize: 11, marginLeft: 4 }}>RWF</span>\r\n                                    </td>\r\n                                  </tr>\r\n                                );\r\n                              })}\r\n                            </tbody>\r\n                            <tfoot>\r\n                              <tr style={{ background: '#1D3557', borderTop: '2px solid #1D3557' }}>\r\n                                <td colSpan={5} style={{ padding: '12px 14px', color: '#CBD5E1', fontWeight: 700, fontSize: 13 }}>\r\n                                  Total Revenue for {new Date(reportDay).toLocaleDateString('en-GB', { day: '2-digit', month: 'short', year: 'numeric', timeZone: 'Africa/Kigali' })}\r\n                                </td>\r\n                                <td style={{ padding: '12px 14px', textAlign: 'right', color: '#FFF', fontWeight: 800, fontSize: 16, whiteSpace: 'nowrap' }}>\r\n                                  {Number(eodRevenue).toLocaleString()} <span style={{ fontSize: 12, opacity: 0.7 }}>RWF</span>\r\n                                </td>\r\n                              </tr>\r\n                            </tfoot>\r\n                          </table>\r\n                        </div>\r\n                      )}\r\n                    </section>\r\n                  );\r\n                })()}\r\n\r\n                {/* \u2500\u2500 Current Stock Levels \u2500\u2500 */}`;

const oldFragment = '<div className="am-eod-report">\r\n                {/* \u2500\u2500 Current Stock Levels \u2500\u2500 */}';
const newContent = content.replace(oldFragment, newSection);

if (newContent === content) {
  console.log('WARN: No replacement made. Trying with LF only...');
  const oldFragment2 = '<div className="am-eod-report">\n                {/* \u2500\u2500 Current Stock Levels \u2500\u2500 */}';
  const newContent2 = content.replace(oldFragment2, newSection);
  if (newContent2 === content) {
    console.log('ERROR: replacement failed!');
    process.exit(1);
  }
  fs.writeFileSync(filepath, newContent2, 'utf8');
  console.log('Written with LF-only match.');
} else {
  fs.writeFileSync(filepath, newContent, 'utf8');
  console.log('Written with CRLF match. Done!');
}
